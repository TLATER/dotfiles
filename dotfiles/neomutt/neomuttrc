set realname = "Tristan Daniël Maat"

set sendmail = "msmtp"
set use_from = yes
set envelope_from = yes

set sidebar_visible
set sidebar_format = "%B%?F? [%F]?%* %?N?%N/?%S"
set mail_check_stats

bind index,pager \CP sidebar-prev
bind index,pager \CN sidebar-next
bind index,pager \CX sidebar-open

set folder = "$XDG_DATA_HOME/mail/"
mailboxes `cd "$XDG_DATA_HOME/mail/" && find codethink.co.uk/* -type d ! \( -name new -or -name cur -or -name tmp \) -printf '="%p" '`
