# Use the message bar instead of the window title for messages on
# alacritty
termcapinfo alacritty "hs@:ts@:fs@:ds@"

# Set the window title to commands automagically
shelltitle "$ |bash"

# Bind the command key to C-o for emacs (and muscle memory) compatability
escape ^Oo

# Nethack!
nethack on

# Set the caption bars
caption always "%{bK}%n %{B}%t%="

# Scrollback
defscrollback 10000
compacthist on

# Enable 256 colors
term screen-256color
attrcolor b ".I"
termcapinfo alacritty 'Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm'
defbce
