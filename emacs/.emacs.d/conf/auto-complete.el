;; ------------------------------------------------------------
;; auto-complete.el
;;
;; Auto complete mode configuration
;; ------------------------------------------------------------

;; Set up autocompletion
(use-package auto-complete
  :config
  (ac-config-default))
