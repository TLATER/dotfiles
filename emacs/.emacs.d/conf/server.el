;; ------------------------------------------------------------
;; server.el
;;
;; Definitions for the emacs server
;; ------------------------------------------------------------

(setq server-window 'pop-to-buffer)
